fun sayHi(first, last) {
  print "Hi, " + first + " " + last + "!";
}

sayHi("Dear", "Reader");

fun fib(n) {
  if (n <= 1) return n;
  return fib(n - 2) + fib(n - 1);
}

// var start = clock();
for (var i = 0; i < 10; i = i + 1) {
  // start = clock();
  print fib(i);
  // print clock() - start;
}